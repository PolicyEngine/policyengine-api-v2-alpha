Household impact jobs now persist the `baseline_job_id` on the reform job's JSON `request_data` column by replacing the dict rather than mutating it in place (SQLAlchemy doesn't track in-place JSON mutations).
